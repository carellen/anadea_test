<%= form_with(model: page, local: true) do |form| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :page_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :page_description %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :page_slug %>
  </div>

  <div class="field">
    <%= form.label :menu_label %>
    <%= form.text_field :menu_label, id: :page_menu_label %>
  </div>

  <div class="field">
    <%= form.label :h1 %>
    <%= form.text_field :h1, id: :page_h1 %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :page_content %>
  </div>

  <div class="field">
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at, id: :page_published_at %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.number_field :priority, id: :page_priority %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
